spring:
  application:
    name: payment-service
  cloud:
    config:
      profile: prod
  config:
    import: optional:configserver:http://localhost:8888



#  datasource:
#    username: postgres
#    password: '12345'
#    url: jdbc:postgresql://localhost:5432/paymentdb-t2
#  jpa:
#    properties:
#      hibernate:
#        dialect: org.hibernate.dialect.PostgreSQLDialect
#    hibernate:
#      ddl-auto: update
#  cloud:
#    openfeign:
#      circuitbreaker:
#        enabled: true
#eureka:
#  client:
#    fetch-registry: true
#    register-with-eureka: true
#    service-url:
#      defaultZone: http://localhost:8761/eureka
#  instance:
#    instance-id: ${spring.application.name}:${random.value}}
#    prefer-ip-address: true
#server:
#  port: 0
